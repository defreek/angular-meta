/*jshint expr: true*/

var should = require('should');

'use strict';

describe('Angular Meta', function(){

  describe('.prettyStringify', function () {
    it('should return a string given an array of objects', function() {
      var one = 1;
      one.should.equal(1);
    });
  });

});
